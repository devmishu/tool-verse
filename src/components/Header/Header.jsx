import NavBar from "../Header/NabBar/NavBar";
import Hero from "./Hero/Hero";

const Header = () => {
    return (
        <header>
            <NavBar />
            <Hero />
            
        </header>
    );
};

export default Header;